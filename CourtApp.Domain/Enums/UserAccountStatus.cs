namespace CourtApp.Domain.Enums
{
    /// <summary>
    /// User account status in the system
    /// </summary>
    public enum UserAccountStatus
    {
        Pending = 1,
        Active = 2,
        Inactive = 3,
        Suspended = 4,
        Deleted = 5
    }
}
