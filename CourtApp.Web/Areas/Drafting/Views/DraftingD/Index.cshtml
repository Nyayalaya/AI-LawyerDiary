@{
    ViewData["Title"] = localizer["Acciedent Claim Petition"];
    ViewData["Caption"] = localizer["Death"];
}
<div class="card">
    <div class="col-sm-12" style="padding:20px">
        @* @if ((AuthorizationService.AuthorizeAsync(User, Permissions.DeathClaimPetition.Create)).Result.Succeeded)
        {
            <a onclick="jQueryModalGet('/drafting/draftingd/OnGetCreateOrEdit','Create Death Claim Petition')" class="btn bg-success">
                <i class="fa fa-plus-square"></i> Create New Petition
            </a>
        } *@
        <a onclick="jQueryModalGet('/drafting/draftingd/OnGetCreateOrEdit','Death Claim Petition Form')" class="btn bg-success">
            <i class="fa fa-plus-square"></i> Add New Petition
        </a>

        <a id="reload" class="btn btn-primary text-white">
            <i class="fa fas fa-bolt"></i>
            Reload
        </a>
    </div>
    <div id="viewAll" class="card-body table-responsive">
    </div>
</div>
@section Scripts
{
    <script src="~/js/site.js"></script>
    <script>
        $(document).ready(function () {
            $("#brandTable").DataTable();
            loadData();
            $('#reload').on('click', function () {
                loadData();
            });           
        });
        function loadData() {
            $('#viewAll').load('/LawyerDiary/BookMaster/LoadAll');
        }
    </script>
}